define([
    'jquery',
    'backbone'
], function ($, Backbone) {

    var StartGame = {

        runStartPage : function (profile) {
            //console.log(profile);
        }
    };

    //Global vents
    /*App.vent.on("create:new:requisition", function () {
        RequisitionAppController.addNewRequisitionForm();
    });*/

    return StartGame;
});